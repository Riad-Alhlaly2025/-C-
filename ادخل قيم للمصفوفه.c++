#include <iostream>//برنامج يطلب من المستخدم  ادخال قيم للمصفوفه
using namespace std;

int main()
{
    int sum=4;
    int opp[sum];
    cout<<"enter i the : (0) ?";
    cin>>opp[0];
    
    cout<<"enter i the : (1) ?";
    cin>>opp[0];
    
    cout<<"enter i the : (2) ?";
    cin>>opp[0];
    
    cout<<"enter i the : (3) ?";
    cin>>opp[0];
    
    cout<<"enter i the : (4) ?";
    cin>>opp[0];
    return 0;
}