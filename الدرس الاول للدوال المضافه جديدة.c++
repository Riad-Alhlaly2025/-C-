#include <iostream>  //    داله اساسيه
using namespace std;  //   داله اساسيه
int diff (int a ,int b) // ووضع لها قوسين(int) داله جديده تحتوي على متغيران من نوع  
{
   return a-b; // الجمله:استرجاع قيمه للمتغيران(a,b)
}
int main() // داله اساسيه
{
    cout<<diff(8,4);// اسم المخزن المعرف للداله الجديده ,وامر طباعه للمتغيران معا تعريفهما

    return 0;
}